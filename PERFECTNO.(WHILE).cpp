#include<stdio.h>
int main()
{
	int i,n,sum=0,d;
	scanf("%d",&n);
	n=d;
	while(i<n)
	{
		if (n%i==0)
		sum=sum+i;
		i++;
	} if(d==sum)
	printf("perfect");
	else
	printf("not a perfect");
}
